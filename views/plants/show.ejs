<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<section>
	<p>Houseplants for: <%= plant.owner.name %></p>
  <h1><%= plant.name %> 🪴</h1>
  <h2>This <%= plant.name %> was watered on: <%= plant.waterDate %></h2>
  <% if (plant.owner.equals(user?.profile._id)) { %>
    <a href="/plants/<%= plant._id %>/edit">
      <button class="btn">Edit this plant 🪴</button>
    </a>
    <form action="/plants/<%= plant._id %>?_method=DELETE" method="POST">
      <button class="btn" type="submit">Delete this plant 🪴</button>
    </form>
  <% } %>
</section>

<%- include('../partials/footer') %>